PORT=8080
ASPNETCORE_ENVIRONMENT=Development
DATABASE_PROVIDER=Sqlite
DATABASE_CONNECTION_STRING=Data Source=order-demo.db

# For Docker + PostgreSQL, override with:
# DATABASE_PROVIDER=Postgres
# DATABASE_CONNECTION_STRING=Host=postgres;Port=5432;Database=orders_demo;Username=postgres;Password=postgres
